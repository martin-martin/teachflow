im trying to build the backend of a homework grading tool for teachers in python. 

This is the idea of a MVP:


so we can roughly split it into:

Raw data (list of students, pdf)

Loop per student:
    Load proper raw data
    ocr pdf to usable text
    use langchain + openai + preset prompt 
    use langchain (+ json the o/i for frontend) to acommodate for adjustments
    save all to json 



Raw Data: 
    json file: indexed list of students
    folder/zip: list of pdfs (homework submissions) renamed to student name/id
    (homeworks are handwritten english essays)

Pipeline:
Per student:
    1. Extract text from pdfs using OCR
    2. pass text to llm to Grade based on preset prompt & give personalized feedback
    3. Have teacher be able to adjust personalized feedback DIRECTLY (direclty editing the text)*
    4. store grade + feedback pair assigned to student in database (or other for mvp)

* During that: Langchain UI? (teammate recommended)

End result:
json per student:
    {
        "student_id": "12345",
        "name": "John Doe",
        "filedirectory": "path/to/pdf",
        "grade": "A",
        "feedback": "Great job on your essay! You demonstrated a strong understanding of the topic and your arguments were well-structured. Keep up the good work!"
    }

Technologies to consider:
    - OCR
    - LLM: for mvp openai
    - Langchain UI for feedback editing
    - Json for database
